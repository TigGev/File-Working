#include <iostream>
#include <fstream>
#include <string>
#include <unordered_map>
#include <sstream>

int main() {
    std::fstream file("./file3.txt");
    std::unordered_map<std::string, bool> buf;
    std::string line;
    
    while (std::getline(filem, line))
}